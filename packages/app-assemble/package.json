{
  "name": "@prisma/app-assemble",
  "version": "0.2.0",
  "type": "module",
  "description": "Deploy-only: routes each service to its build adapter's /assemble entry and applies the wrapper-inlining policy. Consumed by @prisma/app-cli; the future programmatic deploy API is its second consumer.",
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@prisma/app": "workspace:0.2.0"
  },
  "devDependencies": {
    "@prisma/app-tsdown": "workspace:0.2.0",
    "@types/bun": "^1.3.13",
    "@types/node": "^24.10.13",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  },
  "files": [
    "dist",
    "src"
  ],
  "types": "./dist/index.d.mts",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/prisma/app.git",
    "directory": "packages/app-assemble"
  },
  "engines": {
    "node": ">=24"
  },
  "publishConfig": {
    "access": "public"
  }
}
