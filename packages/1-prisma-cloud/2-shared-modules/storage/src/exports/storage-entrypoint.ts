/**
 * Public surface: the reusable storage boot program the service's build points
 * `entry` at. It exports nothing — importing it runs it. Implementation lives
 * in `../execution/storage-entrypoint.ts`.
 */
import '../execution/storage-entrypoint.ts';
