/**
 * Public surface: the reusable email boot program the service's build points
 * `entry` at. It exports nothing — importing it runs it. Implementation
 * lives in `../execution/email-entrypoint.ts`.
 */
import '../execution/email-entrypoint.ts';
