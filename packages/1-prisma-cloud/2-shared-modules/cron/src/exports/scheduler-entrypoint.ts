/**
 * Public surface: the reusable scheduler boot program every app's scheduler
 * build points `entry` at. It exports nothing — importing it runs it.
 * Implementation lives in `../execution/scheduler-entrypoint.ts`.
 */
import '../execution/scheduler-entrypoint.ts';
