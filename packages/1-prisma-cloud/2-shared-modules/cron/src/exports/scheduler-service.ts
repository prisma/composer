/** Public surface: the bare scheduler node the cron build points `module` at. Implementation lives in `../execution/scheduler-service.ts`. */
export { default } from '../execution/scheduler-service.ts';
