{
  "name": "@internal/assemble",
  "version": "0.10.0",
  "type": "module",
  "description": "Deploy-only: routes each service to its build adapter's /assemble entry and applies the wrapper-inlining policy. Consumed by @prisma/composer-cli; the future programmatic deploy API is its second consumer.",
  "exports": {
    ".": "./dist/index.mjs",
    "./package.json": "./package.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "tsdown",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@internal/core": "workspace:0.10.0",
    "@internal/foundation": "workspace:0.10.0"
  },
  "devDependencies": {
    "@internal/tsdown-config": "workspace:0.10.0",
    "@types/bun": "^1.3.13",
    "@types/node": "^26.0.1",
    "tsdown": "^0.22.7",
    "typescript": "^6.0.3"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/prisma/composer.git",
    "directory": "packages/0-framework/3-tooling/assemble"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "private": true,
  "types": "./dist/index.d.mts"
}
