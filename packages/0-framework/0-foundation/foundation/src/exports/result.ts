/** Public surface: the shared Result type (duplicated from prisma/prisma pending extraction). Implementation lives in `../result.ts`. */
export type { NotOk, Ok, Result } from '../result.ts';
export { notOk, ok, okVoid } from '../result.ts';
