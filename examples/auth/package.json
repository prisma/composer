{
  "name": "@prisma/example-auth",
  "version": "0.17.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build src/api/server.ts --target=bun --outfile dist/api/server.mjs && bun build src/ops/server.ts --target=bun --outfile dist/ops/server.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests",
    "smoke:deployed": "( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun scripts/smoke.ts )",
    "deploy": "pnpm turbo run build --filter @prisma/example-auth... && ( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun node_modules/.bin/prisma-composer deploy module.ts ${AUTH_STACK_NAME:+--name \"$AUTH_STACK_NAME\"} )",
    "destroy": "( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun node_modules/.bin/prisma-composer destroy module.ts --production ${AUTH_STACK_NAME:+--name \"$AUTH_STACK_NAME\"} )"
  },
  "dependencies": {
    "@prisma/composer": "workspace:0.17.0",
    "@prisma/composer-cli": "workspace:0.17.0",
    "@prisma/composer-prisma-cloud": "workspace:0.17.0",
    "@prisma/orm-postgres": "8.0.0-rc.8",
    "arktype": "^2.2.3",
    "hono": "^4.12.31"
  },
  "devDependencies": {
    "@prisma/cli-engine": "0.3.0",
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "effect": "4.0.0-rc.112",
    "@effect/sql-d1": "4.0.0-rc.112",
    "@effect/sql-pg": "4.0.0-rc.112",
    "@effect/vitest": "4.0.0-rc.112",
    "@effect/platform-bun": "4.0.0-rc.112",
    "@effect/platform-node": "4.0.0-rc.112",
    "@effect/platform-node-shared": "4.0.0-rc.112",
    "@effect/sql-sqlite-do": "4.0.0-rc.112"
  }
}
