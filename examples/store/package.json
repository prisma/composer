{
  "name": "@prisma/example-store",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'nothing to build for this package itself — the modules build via turbo ^build, from the workspace deps below'",
    "dev": "bun run scripts/dev.ts",
    "typecheck": "tsc --noEmit",
    "deploy": "pnpm turbo run build --filter @prisma/example-store... && ( set -a; . ../../.env; set +a; bun node_modules/.bin/prisma-composer deploy module.ts ${STORE_STACK_NAME:+--name \"$STORE_STACK_NAME\"} )",
    "destroy": "( set -a; . ../../.env; set +a; bun node_modules/.bin/prisma-composer destroy module.ts --production ${STORE_STACK_NAME:+--name \"$STORE_STACK_NAME\"} )"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-rc.111",
    "@prisma/composer": "workspace:0.12.0",
    "@prisma/composer-cli": "workspace:0.12.0",
    "@prisma/composer-prisma-cloud": "workspace:0.12.0",
    "@store/catalog": "workspace:0.12.0",
    "@store/orders": "workspace:0.12.0",
    "@store/promotions": "workspace:0.12.0",
    "@store/storefront": "workspace:0.12.0",
    "alchemy": "2.0.0-beta.74",
    "arktype": "^2.2.3",
    "effect": "4.0.0-rc.111"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "effect": "4.0.0-rc.111",
    "@effect/sql-d1": "4.0.0-rc.111",
    "@effect/sql-pg": "4.0.0-rc.111",
    "@effect/vitest": "4.0.0-rc.111",
    "@effect/platform-bun": "4.0.0-rc.111",
    "@effect/platform-node": "4.0.0-rc.111",
    "@effect/platform-node-shared": "4.0.0-rc.111"
  }
}
