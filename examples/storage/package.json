{
  "name": "@prisma/example-storage",
  "version": "0.14.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist/blobs && bun build src/blobs/server.ts --target=bun --outfile dist/blobs/server.mjs",
    "typecheck": "tsc --noEmit",
    "test": "bun test tests",
    "deploy": "pnpm turbo run build --filter @prisma/example-storage... && ( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun node_modules/.bin/prisma-composer deploy module.ts ${STORAGE_STACK_NAME:+--name \"$STORAGE_STACK_NAME\"} )",
    "destroy": "( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun node_modules/.bin/prisma-composer destroy module.ts --production ${STORAGE_STACK_NAME:+--name \"$STORAGE_STACK_NAME\"} )"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1085.0",
    "@prisma/composer": "workspace:0.14.0",
    "@prisma/composer-prisma-cloud": "workspace:0.14.0",
    "alchemy": "2.0.0-beta.74",
    "effect": "4.0.0-rc.111"
  },
  "devDependencies": {
    "@prisma/composer": "workspace:0.14.0",
    "@prisma/composer-cli": "workspace:0.14.0",
    "@prisma/composer-prisma-cloud": "workspace:0.14.0",
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "effect": "4.0.0-rc.111",
    "@effect/sql-d1": "4.0.0-rc.111",
    "@effect/sql-pg": "4.0.0-rc.111",
    "@effect/vitest": "4.0.0-rc.111",
    "@effect/platform-bun": "4.0.0-rc.111",
    "@effect/platform-node": "4.0.0-rc.111",
    "@effect/platform-node-shared": "4.0.0-rc.111"
  }
}
