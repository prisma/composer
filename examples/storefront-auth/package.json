{
  "name": "@prisma/example-storefront-auth",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'nothing to build for this package itself — @storefront-auth/auth and @storefront-auth/storefront build via turbo ^build, from the workspace deps below'",
    "typecheck": "tsc --noEmit",
    "deploy": "pnpm turbo run build --filter @prisma/example-storefront-auth... && ( set -a; . ../../.env; set +a; bun node_modules/.bin/prisma-app deploy system.ts ${STOREFRONT_STACK_NAME:+--name \"$STOREFRONT_STACK_NAME\"} )",
    "destroy": "( set -a; . ../../.env; set +a; bun node_modules/.bin/prisma-app destroy system.ts ${STOREFRONT_STACK_NAME:+--name \"$STOREFRONT_STACK_NAME\"} )"
  },
  "dependencies": {
    "@effect/platform-bun": "4.0.0-beta.92",
    "@prisma/app": "workspace:*",
    "@prisma/app-nextjs": "workspace:*",
    "@prisma/app-node": "workspace:*",
    "@prisma/alchemy": "workspace:*",
    "@prisma/app-cloud": "workspace:*",
    "@prisma/app-rpc": "workspace:*",
    "@storefront-auth/auth": "workspace:*",
    "@storefront-auth/storefront": "workspace:*",
    "alchemy": "2.0.0-beta.59",
    "arktype": "^2.2.3",
    "effect": "4.0.0-beta.92"
  },
  "devDependencies": {
    "@prisma/app-cli": "workspace:*",
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
