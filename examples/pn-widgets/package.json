{
  "name": "@prisma/example-pn-widgets",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && bun build src/server.ts --target=bun --outfile dist/server.mjs",
    "typecheck": "tsc --noEmit",
    "deploy": "pnpm turbo run build --filter @prisma/example-pn-widgets... && ( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun node_modules/.bin/prisma-composer deploy module.ts ${PN_WIDGETS_STACK_NAME:+--name \"$PN_WIDGETS_STACK_NAME\"} )",
    "destroy": "( set -a; . \"${PRISMA_DEPLOY_ENV:-../../.env}\"; set +a; bun node_modules/.bin/prisma-composer destroy module.ts --production ${PN_WIDGETS_STACK_NAME:+--name \"$PN_WIDGETS_STACK_NAME\"} )"
  },
  "dependencies": {
    "@prisma-next/postgres": "0.15.0",
    "@prisma/composer": "workspace:0.2.0",
    "@prisma/composer-prisma-cloud": "workspace:0.2.0",
    "effect": "4.0.0-beta.93",
    "pg": "8.22.0"
  },
  "devDependencies": {
    "@prisma/composer": "workspace:0.2.0",
    "@prisma/management-api-sdk": "^1.47.0",
    "@types/bun": "^1.3.13",
    "typescript": "^6.0.3"
  }
}
