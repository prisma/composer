# The topology design moved

The canonical design lives in **pdp-control-plane** at `projects/branch-topology/spec.md` (branch `claude/branch-topology-design`), with its division of work in `plan.md` beside it. Composer's own work items from that plan are mirrored in this project's [plan.md](plan.md) follow-up section.

This file is a pointer, not a mirror — the mirror kept drifting and the design has one home now.
