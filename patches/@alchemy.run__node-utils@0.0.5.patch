diff --git a/lib/lockfile.js b/lib/lockfile.js
index e35a8f312b5ecd70bf808071ab53a8d3cff42435..fb5f73acc4084dc2a9fa061fc63c5dada0ec1708 100644
--- a/lib/lockfile.js
+++ b/lib/lockfile.js
@@ -4,6 +4,29 @@ import { exitHook } from "./exit-hook.js";
 import * as mtimePrecision from "./mtime-precision.js";
 import * as retry from "./retry.js";
 const locks = {};
+const locksOwnedForExit = new Set();
+let removeExitHook;
+function removeOwnedLocksOnExit() {
+    for (const lock of locksOwnedForExit) {
+        try {
+            lock.options.fs.rmdirSync(lock.lockfilePath);
+        }
+        catch {
+            /* Empty */
+        }
+    }
+}
+function trackLockForExit(lock) {
+    locksOwnedForExit.add(lock);
+    removeExitHook ??= exitHook(removeOwnedLocksOnExit);
+}
+function untrackLockForExit(lock) {
+    locksOwnedForExit.delete(lock);
+    if (locksOwnedForExit.size === 0) {
+        removeExitHook?.();
+        removeExitHook = undefined;
+    }
+}
 function getLockFile(file, options) {
     return options.lockfilePath || `${file}.lock`;
 }
@@ -141,6 +164,9 @@ function updateLock(file, options) {
     }
 }
 function setLockAsCompromised(file, lock, err) {
+    if (lock.released) {
+        return;
+    }
     lock.released = true;
     // Cancel lock mtime update
     /* istanbul ignore if */
@@ -150,6 +176,7 @@ function setLockAsCompromised(file, lock, err) {
     if (locks[file] === lock) {
         delete locks[file];
     }
+    untrackLockForExit(lock);
     lock.options.onCompromised(err);
 }
 // ----------------------------------------------------------
@@ -203,6 +230,7 @@ export function lock(file, options, callback) {
                     options: resolved,
                     lastUpdate: Date.now(),
                 });
+                trackLockForExit(internalLock);
                 // We must keep the lock fresh to avoid staleness
                 updateLock(file, resolved);
                 callback(null, (releasedCallback) => {
@@ -241,7 +269,10 @@ export function unlock(file, options, callback) {
         }
         lock.released = true;
         delete locks[file];
-        removeLock(file, resolved, callback);
+        removeLock(file, resolved, (err) => {
+            untrackLockForExit(lock);
+            callback(err);
+        });
     });
 }
 export function check(file, options, callback) {
@@ -271,17 +302,4 @@ export function check(file, options, callback) {
 export function getLocks() {
     return locks;
 }
-// Remove acquired locks on exit
-/* istanbul ignore next */
-exitHook(() => {
-    for (const file in locks) {
-        const options = locks[file].options;
-        try {
-            options.fs.rmdirSync(getLockFile(file, options));
-        }
-        catch (e) {
-            /* Empty */
-        }
-    }
-});
 //# sourceMappingURL=lockfile.js.map
\ No newline at end of file
diff --git a/src/lockfile.ts b/src/lockfile.ts
index 27deca4f423ffce3aecacecbdd8e02f231a52891..99cc60161a5c0ccc9be6e7dc78716769afe20205 100644
--- a/src/lockfile.ts
+++ b/src/lockfile.ts
@@ -70,6 +70,32 @@ type AcquireCallback = (
 ) => void;
 
 const locks: Record<string, InternalLock> = {};
+const locksOwnedForExit = new Set<InternalLock>();
+let removeExitHook: (() => void) | undefined;
+
+function removeOwnedLocksOnExit(): void {
+  for (const lock of locksOwnedForExit) {
+    try {
+      lock.options.fs.rmdirSync(lock.lockfilePath);
+    } catch {
+      /* Empty */
+    }
+  }
+}
+
+function trackLockForExit(lock: InternalLock): void {
+  locksOwnedForExit.add(lock);
+  removeExitHook ??= exitHook(removeOwnedLocksOnExit);
+}
+
+function untrackLockForExit(lock: InternalLock): void {
+  locksOwnedForExit.delete(lock);
+
+  if (locksOwnedForExit.size === 0) {
+    removeExitHook?.();
+    removeExitHook = undefined;
+  }
+}
 
 function getLockFile(file: string, options: { lockfilePath?: string }): string {
   return options.lockfilePath || `${file}.lock`;
@@ -302,6 +328,10 @@ function setLockAsCompromised(
   lock: InternalLock,
   err: Error,
 ): void {
+  if (lock.released) {
+    return;
+  }
+
   lock.released = true;
 
   // Cancel lock mtime update
@@ -314,6 +344,7 @@ function setLockAsCompromised(
     delete locks[file];
   }
 
+  untrackLockForExit(lock);
   lock.options.onCompromised(err);
 }
 
@@ -382,6 +413,7 @@ export function lock(
           options: resolved,
           lastUpdate: Date.now(),
         });
+        trackLockForExit(internalLock);
 
         // We must keep the lock fresh to avoid staleness
         updateLock(file, resolved);
@@ -443,7 +475,10 @@ export function unlock(
     lock.released = true;
     delete locks[file];
 
-    removeLock(file, resolved, callback);
+    removeLock(file, resolved, (err) => {
+      untrackLockForExit(lock);
+      callback(err);
+    });
   });
 }
 
@@ -486,17 +521,3 @@ export function check(
 export function getLocks(): Record<string, InternalLock> {
   return locks;
 }
-
-// Remove acquired locks on exit
-/* istanbul ignore next */
-exitHook(() => {
-  for (const file in locks) {
-    const options = locks[file].options;
-
-    try {
-      options.fs.rmdirSync(getLockFile(file, options));
-    } catch (e) {
-      /* Empty */
-    }
-  }
-});
